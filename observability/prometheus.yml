global:
  scrape_interval: 20s
  evaluation_interval: 20s

scrape_configs:
  # 按 CN/DN 多节点动态发现：每个端点一个 targets/*.json
  - job_name: otb_cluster_nodes
    file_sd_configs:
      - files:
          - /etc/prometheus/targets/*.json
        refresh_interval: 15s
